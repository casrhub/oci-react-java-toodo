version: 0.1
component: build

steps:
  - type: Command
    name: Docker Hub Login
    command: |

  - type: Command
    name: Build & Push Docker Image
    command: |
      docker build \
        -t casrhub/todolistapp-springboot:0.1 \
        -f MtdrSpring/backend/Dockerfile \
        MtdrSpring/backend

      docker push casrhub/todolistapp-springboot:0.1


#test
